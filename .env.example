# =============================================================================
# LearnSynth Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration (Required)
# -----------------------------------------------------------------------------
# Get these from your Supabase project dashboard
# https://app.supabase.com/project/[your-project]/settings/api
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# -----------------------------------------------------------------------------
# AI Configuration (Required)
# -----------------------------------------------------------------------------
# OpenAI API Key for AI features (lesson generation, AI tutor, etc.)
OPENAI_API_KEY=your_openai_api_key_here

# LLM Provider (default: openai)
LLM_PROVIDER=openai
LLM_MODEL=gpt-3.5-turbo

# TTS Provider (optional - for text-to-speech features)
TTS_PROVIDER=stub
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# Embeddings Provider (optional - for semantic search)
EMBEDDING_PROVIDER=stub
HUGGINGFACE_API_KEY=your_huggingface_api_key_here

# -----------------------------------------------------------------------------
# Backend Configuration
# -----------------------------------------------------------------------------
NODE_ENV=development
PORT=4000
FRONTEND_URL=http://localhost:5173

# Redis Configuration (optional - for caching)
# If you want to use Redis locally with Docker, uncomment in docker-compose.yml
REDIS_URL=redis://redis:6379
REDIS_ENABLE_CACHING=true
REDIS_DEFAULT_TTL=3600

# Vector Database Mode (default: supabase)
VECTOR_DB_MODE=supabase

# Rate Limiting (optional)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging Level (optional)
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# Frontend Environment
# -----------------------------------------------------------------------------
# These are read by Vite at build time
VITE_API_URL=http://localhost:4000
VITE_WS_URL=http://localhost:4000

# -----------------------------------------------------------------------------
# Production Configuration Notes
# -----------------------------------------------------------------------------
# When deploying to production:
# 1. Change all default values
# 2. Use strong, unique secrets
# 3. Use Supabase production database
# 4. Enable SSL/TLS
# 5. Set proper CORS origins
# 6. Use environment-specific configurations

